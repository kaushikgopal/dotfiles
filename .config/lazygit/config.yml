gui:
  # Keep lazygit's native theme; only override author color.
  authorColors:
    '*': green

git:
  pagers:
    - pager: ~/.config/git/delta-theme --paging=never --line-numbers --hyperlinks --hyperlinks-file-link-format="lazygit-edit://{path}:{line}"
