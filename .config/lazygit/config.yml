gui:
  # Keep lazygit's native theme; only override author color.
  authorColors:
    '*': green

git:
  pagers:
    - pager: delta-theme --paging=never --line-numbers --hyperlinks --hyperlinks-file-link-format="lazygit-edit://{path}:{line}"
