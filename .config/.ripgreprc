# Don't let ripgrep vomit really long lines to my terminal, and show a preview.
--max-columns=150
--max-columns-preview

# Add my 'web' type.
--type-add
web:*.{html,css,js}*


# Search hidden files / directories (e.g. dotfiles) by default
# -u    = '--no-ignore'                     # don't respect .ignore files
# -uu   = '--no-ignore --hidden'.           # don't respect .gitignore either
# -uuu  = '--no-ignore --hidden --binary'   # don't even ignore hidden files/directories

# Using glob patterns to include/exclude files or folders
# --glob
# !.git/*

# Set the colors.
--colors=line:none
--colors=line:style:bold

# Because who cares about case!?
--smart-case

# -S = --smart-case
# -I = --no-filename

# --no-line-number
# --color=never  # when you want to script

#############################################################
# MANUAL
# sample commands
#############################################################

# search for string "fast" in file README.md
# rg fast README.md

# search patterns recursively from current directory
# rg "com\.android\.application"


# replace fast -> FAST
# rg fast README.md -r FAST


# ------------
### File types

# glob pattern only applies to the the filepath
# search all rust files for "fun run"
# rg 'fn run' -g '*.rs'
# rg 'fn run' -g '!*.rs'  # invert


# - simpler alternative for file types
#            --type rust
#             -trust
#            --type-not rust

# search pattern
# - only search gradle files
# - exclude experimental folder
# rg "com\.android\.application" --type gradle  -g '!experimental/*'


# -----------------------
### Print filenames only (don't show content results)
# - but still search based on content search results

# search files that contain foo and print filenames only
# rg foo --files-with-matches
#        -l

# rg foo --files-without-match
#        # invert (i.e. show filenames without the pattern)


# ------------------------------------
## Searching based on filename/path

# list all files in current directory
# rg --files

# exclude pattern (from file name/path)
# rg .git/hooks -uuu --files | rg -v '\.sample'
#               -uuu necessary  cause you're searching .git which is ignored by default