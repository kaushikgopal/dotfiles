[include]
    path = ~/.config/git/config

[includeIf "gitdir:~/src/off/"]
    path = ~/.gitconfig-off
[includeIf "gitdir:~/src/oss/"]
    path = ~/.gitconfig-oss
[includeIf "gitdir:~/src/per/"]
    path = ~/.gitconfig-per